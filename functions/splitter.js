const fs = require('fs');
const path = require('path');

// Функция для деления файла на части по 80 МБ
async function splitFile(inputFilePath, outputDir) {
    return new Promise((resolve, reject) => {
        
    })
}